import type { OpenClawPluginApi } from "../../src/plugins/types.js";

export default function register(_api: OpenClawPluginApi) {
  // MCP Hub config is consumed by gateway server methods directly.
}
